# LLM Ambiguity & Connections Puzzles

> **Project direction (July 2026):** this work is evolving into
> **"Mind the Gap: Adversarial Synthesis of Human-Easy, Machine-Hard Word Puzzles"** —
> see [PROPOSAL.md](PROPOSAL.md). Everything below (validated difficulty metric, datasets,
> solver harness, generation pipeline) carries over as infrastructure and baselines for
> that project.

## Tracking and Amplifying Linguistic Ambiguity in LLM-Generated Connections Puzzles

Code, data, and paper for a framework that generates NYT *Connections*-style puzzles
with controlled, amplified linguistic ambiguity — and, critically, **validates the
difficulty metric used to evaluate them against ground truth** instead of assuming it.

Every number, table, and figure in the paper regenerates from this repository.

## Headline results (all reproducible below; data current through 2026-07-04)

- **Difficulty-metric validation** — on 830 official NYT puzzles (3,320 groups with
  ground-truth difficulty levels 0–3), intra-group embedding similarity correlates with
  official difficulty at **Spearman ρ = −0.441** (p < 1e−157), decreases monotonically
  per level (0.337 → 0.311 → 0.284 → 0.245), and orders within-puzzle group pairs
  correctly **70%** of the time (chance 50%).
- **Ambiguity ladder realized** — across the pipeline's four tiers, mean WordNet sense
  counts rise 6.1 → 16.4 → 14.9 → 25.0 and idiomatic multi-word phrases rise 0% → 50%,
  while real NYT groups sit at ~7.2 senses regardless of official level.
- **Harder groups** — pipeline groups score 0.275 ± 0.126 mean intra-group similarity
  vs 0.289 ± 0.081 for real NYT groups (Mann-Whitney p = 1.5e−3) and 0.299 ± 0.097 for
  single-prompt GPT-4o generation (p = 2.7e−4).
- **The official game is drifting harder** — NYT groups published after Sept 2025
  average 0.275 similarity vs 0.294 before (p = 1.6e−9), converging toward the
  semantic-diversity regime the pipeline targets.

## Repository layout

```
├── paper/                        # Revised paper (ACL format)
│   ├── main.tex, refs.bib        # source (compiles with tectonic or pdflatex+bibtex)
│   ├── main.pdf                  # compiled paper
│   ├── acl.sty, acl_natbib.bst   # official ACL style files
│   └── figures/                  # PDFs generated by metrics/make_figures.py
├── pipeline/
│   └── generate_puzzles.py       # 3-stage generator (Least-to-Most CoT → WordNet ReAct → RCoT audit)
├── prompts/                      # ALL prompts, verbatim, versioned
│   ├── stage1_system.txt, stage1_group{1..4}.txt
│   ├── rcot_system.txt, rcot_template.txt
│   └── solver_{direct,cot,tot}.txt
├── metrics/
│   ├── difficulty.py             # embedding difficulty metric D(G) (Eq. 1 of the paper)
│   ├── validate_difficulty.py    # validates D(G) against official NYT levels
│   ├── ambiguity_metrics.py      # WordNet sense counts / ambiguity ratios by tier
│   ├── compare_datasets.py       # real vs synthetic vs pipeline distributions
│   └── make_figures.py           # regenerates every paper figure
├── experiments/
│   └── solve_puzzles.py          # solver harness (direct / CoT / ToT), needs OPENAI_API_KEY
├── results/                      # committed outputs backing the paper's numbers
│   ├── metric_validation.json
│   ├── ambiguity_metrics.json
│   ├── dataset_comparison.json
│   └── figures/                  # PNG previews
├── data/gpt4o/json_files/        # 50-puzzle pipeline corpus (all stage-wise artifacts)
├── data/update_real_data.py      # refreshes the real dataset from the public archive
├── Real_Data.json                # 1,114 official NYT boards (16 words each, seeded shuffle)
├── Real_Data_Ans.json            # official solutions; levels 0-3 recorded through 2025-09-19
├── Training_Data.json            # 2,000 single-prompt GPT-4o synthetic puzzles (baseline)
├── Testing_Data.json             # 400 held-out synthetic puzzles
├── Llama_Model_*.ipynb           # Llama fine-tuning notebooks (real / synthetic / mixed)
└── REVIEWER_RESPONSE.md          # point-by-point mapping of review feedback → fixes
```

**Dataset provenance:** real puzzles come from the community-maintained
[NYT-Connections-Answers](https://github.com/Eyefyre/NYT-Connections-Answers) archive
(puzzles from 2023-06-12 onward; the archive stops recording official difficulty labels
after 2025-09-19), used for non-commercial research. Refresh with
`python data/update_real_data.py`, then re-run the metrics below.

## Reproduce every number in the paper

```bash
pip install -r requirements.txt
python -c "import nltk; nltk.download('wordnet')"

# Table 1 / Figure 2 — metric validation against official NYT levels
python metrics/validate_difficulty.py

# Table 2 / Figure 3 — ambiguity ladder over the 50-puzzle pipeline corpus
python metrics/ambiguity_metrics.py

# Table 3 / Figure 4 — real vs synthetic vs pipeline difficulty
python metrics/compare_datasets.py

# regenerate all paper figures from the results/ JSONs
python metrics/make_figures.py
```

The first embedding run downloads `all-mpnet-base-v2` (~420 MB) from Hugging Face.

## Generate new puzzles

```bash
export OPENAI_API_KEY=sk-...
python pipeline/generate_puzzles.py --num-puzzles 50 --model gpt-4o --out-dir data/generated
```

Each puzzle writes three JSON artifacts (stage 1 generation trace, stage 2 WordNet
verification, stage 3 RCoT audit).

## Evaluate solvers

```bash
export OPENAI_API_KEY=sk-...
# real NYT puzzles, CoT prompting
python experiments/solve_puzzles.py --dataset real --model gpt-4o --strategy cot --limit 100
# generated puzzles, ToT prompting
python experiments/solve_puzzles.py --dataset generated --file Testing_Data.json --strategy tot
```

Reports puzzle solve rate, group accuracy, and per-official-level accuracy, with full
per-instance logs under `results/solver/`. The paper deliberately reports no solver
numbers until this harness has been run end-to-end (see REVIEWER_RESPONSE.md).

## Compile the paper

```bash
cd paper
tectonic main.tex          # or: pdflatex main && bibtex main && pdflatex main && pdflatex main
```

## Citation

Submission in preparation. Earlier version reviewed at NAACL-SRW 2025 (submission 110);
see `REVIEWER_RESPONSE.md` for the point-by-point revision record.

## License

See [LICENSE](LICENSE).
